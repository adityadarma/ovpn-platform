# ============================================================
# VPN Manager — Development Docker Compose
# ============================================================
# Usage:
#   docker compose -f docker-compose.dev.yml up
#   docker compose -f docker-compose.dev.yml --profile postgres up
#   docker compose -f docker-compose.dev.yml --profile mysql up
# ============================================================

services:
  # ── Development Databases ──
  postgres:
    image: postgres:16-alpine
    container_name: vpn-postgres-dev
    environment:
      POSTGRES_USER: vpn
      POSTGRES_PASSWORD: vpn_dev
      POSTGRES_DB: vpn_dev
    volumes:
      - postgres_dev_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U vpn" ]
      interval: 5s
      timeout: 3s
      retries: 5
    profiles: [ postgres ]
    networks:
      - vpn-dev-network

  mariadb:
    image: mariadb:11
    container_name: vpn-mariadb-dev
    environment:
      MARIADB_USER: vpn
      MARIADB_PASSWORD: vpn_dev
      MARIADB_DATABASE: vpn_dev
      MARIADB_ROOT_PASSWORD: root_dev
    volumes:
      - mariadb_dev_data:/var/lib/mysql
    ports:
      - "3306:3306"
    healthcheck:
      test: [ "CMD", "healthcheck.sh", "--connect", "--innodb_initialized" ]
      interval: 5s
      timeout: 3s
      retries: 5
    profiles: [ mysql ]
    networks:
      - vpn-dev-network

  # ── Development API — Hot-reload with tsx watch ──
  api:
    image: node:24-alpine
    container_name: vpn-api-dev
    working_dir: /app
    command: sh -c "npm install -g pnpm && pnpm install && pnpm --filter @vpn/api dev"
    environment:
      NODE_ENV: development
      PORT: "3001"
      JWT_SECRET: dev-secret-please-change-in-production-32chars
      JWT_EXPIRES_IN: 7d
      NODE_REGISTRATION_KEY: ${NODE_REGISTRATION_KEY}
      VPN_TOKEN: ${VPN_TOKEN}
      DATABASE_TYPE: ${DATABASE_TYPE:-sqlite}
      DATABASE_URL: ${DATABASE_URL}
      DATABASE_SQLITE_PATH: /data/vpn.sqlite
    ports:
      - "3001:3001"
    volumes:
      - .:/app
      - api_dev_data:/data
      - pnpm_store:/root/.local/share/pnpm/store
      - node_modules_cache:/app/node_modules
    depends_on:
      postgres:
        condition: service_healthy
        required: false
      mariadb:
        condition: service_healthy
        required: false
    healthcheck:
      test: [ "CMD-SHELL", "wget -qO- http://localhost:3001/api/v1/health || exit 1" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - vpn-dev-network

  # ── Development Web UI — Next.js dev server with hot-reload ──
  web:
    image: node:24-alpine
    container_name: vpn-web-dev
    working_dir: /app
    command: sh -c "npm install -g pnpm && pnpm install && pnpm --filter @vpn/web dev"
    environment:
      NODE_ENV: development
      NEXT_PUBLIC_API_URL: http://localhost:3001
      WATCHPACK_POLLING: "true"
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - pnpm_store:/root/.local/share/pnpm/store
      - node_modules_cache:/app/node_modules
      - /app/apps/web/.next
    depends_on:
      api:
        condition: service_healthy
    networks:
      - vpn-dev-network

  # ── Development Agent (optional) ──
  agent:
    image: node:24-alpine
    container_name: vpn-agent-dev
    working_dir: /app
    command: sh -c "npm install -g pnpm && pnpm install && pnpm --filter @vpn/agent dev"
    environment:
      NODE_ENV: development
      AGENT_MANAGER_URL: ${AGENT_MANAGER_URL:-http://api:3001}
      AGENT_NODE_ID: ${AGENT_NODE_ID:-dev-node-1}
      AGENT_SECRET_TOKEN: ${AGENT_SECRET_TOKEN:-dev-token}
      AGENT_POLL_INTERVAL_MS: 5000
      AGENT_HEARTBEAT_INTERVAL_MS: 30000
    volumes:
      - .:/app
      - pnpm_store:/root/.local/share/pnpm/store
      - node_modules_cache:/app/node_modules
    network_mode: host
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun
    profiles: [ agent ]

networks:
  vpn-dev-network:
    driver: bridge

volumes:
  postgres_dev_data:
  mariadb_dev_data:
  api_dev_data:
  pnpm_store:
  node_modules_cache:
